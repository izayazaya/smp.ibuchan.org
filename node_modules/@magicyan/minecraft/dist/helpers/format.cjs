'use strict';

const _ = require('lodash');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e.default : e; }

const ___default = /*#__PURE__*/_interopDefaultCompat(_);

const convertKeysToCamelCase = (obj) => {
  if (___default.isArray(obj)) {
    return obj.map(convertKeysToCamelCase);
  } else if (___default.isObject(obj)) {
    return ___default.mapKeys(
      ___default.mapValues(obj, convertKeysToCamelCase),
      (__, key) => ___default.camelCase(key)
    );
  }
  return obj;
};

exports.convertKeysToCamelCase = convertKeysToCamelCase;
